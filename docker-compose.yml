version: "3.9"

services:
  kilimall-recon:
    build: .
    container_name: kilimall-recon
    restart: unless-stopped
    ports:
      - "8501:8501"
    environment:
      - DB_DIR=/app/data
    volumes:
      - kilimall_data:/app/data

volumes:
  kilimall_data:
